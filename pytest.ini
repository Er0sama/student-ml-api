[pytest]
# Without this, `pytest` cannot import app.py from the repository root.
# Running `python -m pytest` masks the problem locally because that form
# adds the working directory to sys.path.
pythonpath = .
testpaths = tests
